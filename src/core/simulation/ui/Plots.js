export class Plots {
    constructor(model) {
        this.model = model;
    }
    
    setup(container) {
        // Will add plots here later
    }
    
    update() {
        // Will update plots based on model state later
    }
}
